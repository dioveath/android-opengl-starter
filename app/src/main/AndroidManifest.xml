<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.charicha.opengl3dstarter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".OpenGL3DStarter">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".Vertices3Test"
            android:label="Vertices3 Test"/>
        <activity android:name=".ZBlendingTest"
            android:label="ZBlending Test"/>
        <activity android:name=".CubeTest"
            android:label="Cube Test"/>
        <activity android:name=".HierarchicalTest"
            android:label="Hierarchical Test"/>
    </application>
</manifest>
